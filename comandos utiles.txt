sudo docker compose build // buildea docker
sudo docker compose up // levanta app en http://localhost:3000/

docker run -p 3000:3000 `
>>   -v "$(PWD)/src:/app/src" `
>>   -v "$(PWD)/public:/app/public" `
>>   -v "/app/node_modules" `
>>   -e CHOKIDAR_USEPOLLING=true `
>>   -e WATCHPACK_POLLING=true `
>>   -e FAST_REFRESH=true `
>>   --name lunai-dev `
>>   lunai-frontend


Sin docker:
Primero vamos a la carpeta backend:
-venv\Scripts\activate
-pip install -r requirements.txt (Con hacerlo una vez esta bien, para proximas no es necesario)
-python app.py
Abro una nueva terminal y voy a frontend ("lunai"):
-npm install (Con hacerlo una vez esta bien, para proximas no es necesario)
-npm start
Abrir http://localhost:3000


Posible error axios y react-router-dom:
-npm install react-router-dom axios

Para checkear la base de datos:
-cd backend
-venv\Scripts\activate
-python check_db.py